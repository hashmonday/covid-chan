{
  "routes": [
    {
      "method": "GET",
      "path": "/covid-tests",
      "handler": "covid-tests.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/covid-tests/count",
      "handler": "covid-tests.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/covid-tests/:id",
      "handler": "covid-tests.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/covid-tests",
      "handler": "covid-tests.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/covid-tests/:id",
      "handler": "covid-tests.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/covid-tests/:id",
      "handler": "covid-tests.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
